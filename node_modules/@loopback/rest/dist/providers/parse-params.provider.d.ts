/// <reference types="express" />
import { Provider } from '@loopback/context';
import { RequestBodyParser } from '../body-parsers';
import { ResolvedRoute } from '../router';
import { ParseParams, Request, RequestBodyParserOptions } from '../types';
/**
 * Provides the function for parsing args in requests at runtime.
 *
 * @returns The handler function that will parse request args.
 */
export declare class ParseParamsProvider implements Provider<ParseParams> {
    private requestBodyParser;
    private options;
    constructor(requestBodyParser: RequestBodyParser, options?: RequestBodyParserOptions);
    value(): (request: Request<import("express-serve-static-core").ParamsDictionary>, route: ResolvedRoute) => Promise<import("../types").OperationArgs>;
}
